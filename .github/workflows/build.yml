name: Build

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]
  workflow_dispatch:

jobs:
  build-windows:
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm
          cache-dependency-path: pdf-invoice-diff/package-lock.json

      - uses: dtolnay/rust-toolchain@stable

      - name: Install frontend deps
        working-directory: pdf-invoice-diff
        run: npm ci

      - name: Build Windows bundle
        working-directory: pdf-invoice-diff
        run: npm run tauri build

      - uses: actions/upload-artifact@v4
        with:
          name: windows-bundle
          path: pdf-invoice-diff/src-tauri/target/release/bundle/

  build-macos:
    runs-on: macos-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm
          cache-dependency-path: pdf-invoice-diff/package-lock.json

      - uses: dtolnay/rust-toolchain@stable

      - name: Install frontend deps
        working-directory: pdf-invoice-diff
        run: npm ci

      - name: Build macOS bundle
        working-directory: pdf-invoice-diff
        run: npm run tauri build

      - uses: actions/upload-artifact@v4
        with:
          name: macos-bundle
          path: pdf-invoice-diff/src-tauri/target/release/bundle/
